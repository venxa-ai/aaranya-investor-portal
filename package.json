{
  "name": "aaranya-investor-portal",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "mkdir -p dist && cp index.html dist/index.html",
    "lint": "echo 'No lint required for single HTML file'"
  },
  "dependencies": {
    "vite": "^6.2.3"
  }
}
